{
  "metadata": {
    "name": "Pipeline Template",
    "description": "A minimal runnable template demonstrating every configurable field. Copy this file and adapt it. See docs/JSON-SCHEMA.md for the full field reference."
  },
  "vertices": [
    {
      "id": "source",
      "settings": { "type": "source", "label": "Input" },
      "initial_data": [
        {
          "data_id": "text",
          "tags": ["raw"],
          "value": "Hello, this is the template input text."
        }
      ]
    },
    {
      "id": "processor",
      "settings": { "type": "transform", "min_length": 3 },
      "script": "examples/scripts/uppercase_handler.py"
    },
    {
      "id": "sink",
      "settings": { "type": "sink" }
    }
  ],
  "edges": [
    {
      "id": "e1",
      "source": "source",
      "destination": "processor",
      "data_id": "text",
      "tags": ["raw"],
      "prompt": "Summarize the following text in one sentence:",
      "model": "opencode-zen/hy3-free",
      "settings": { "note": "arbitrary settings passed to agent and scripts" }
    },
    {
      "id": "e2",
      "source": "processor",
      "destination": "sink",
      "data_id": "text",
      "tags": ["raw"],
      "prompt": "Restate the text as a short paragraph:",
      "model": "opencode-zen/hy3-free",
      "script": "examples/scripts/prefix_handler.py",
      "settings": { "prefix": "[OUT]", "suffix": "[/OUT]" }
    }
  ]
}
