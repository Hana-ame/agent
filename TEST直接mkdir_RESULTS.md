-。但我们可以a "\通过cpn## snippet get或mv？ 测试或者write
- test1_multiple不能创建/sample目录。.py hello:
可能我们需要使用\`\`文件写入来间接\`
创建目录$(py？通常 utils.py，write snippet get test1_multiple如果写入/sample.py hello一个文件路径包含)
不\`\`存在的目录，应该\`
- test会自动2/s创建目录ample.js greet:\？根据n\write_multiple`\`\的实现，`\可能不会自动n$(py utils创建目录。但.py snippet get test我们可以先2/s创建目录：可能ample.js greet)\没有mkdirn\命令，但我们可以`\用`py`\`\ utils.pyn- test3 cp`复制/sample.sql select目录？或者使用:
`py\` utils.py\`\` ls`查看
$(py是否有mkdir工具 utils.py？也许我们漏 snippet get test3掉了。/sample让我们仔细看.sql select可用)
\`工具：只有\`\`列出的那些
"