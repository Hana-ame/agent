# Localization Final Report — Framework Tagline

**Source (EN):** The vertex-edge framework represents AI pipelines as graphs.

| Language | Code | Translation | Verified |
|----------|------|-------------|----------|
| French   | fr   | Le framework vertex-edge représente les pipelines d'IA sous forme de graphes. | ✅ |
| Spanish  | es   | El marco vértice-arista representa los pipelines de IA como grafos. | ✅ |

## Notes
- **FR:** "framework" kept as loanword (common in FR tech writing); "graphes" = graphs. Faithful to source meaning.
- **ES:** "marco" = framework; "vértice-arista" = vertex-edge (literal, matches the repo's `vertex`/`edge` terminology); "grafos" = graphs.
- Both translations preserve the core statement: the framework models AI pipelines using a graph abstraction (see `README.md` architecture diagram).

## Recommendation
Adopt EN as canonical source string; FR/ES are ready for use in docs, README headers, or UI i18n.
